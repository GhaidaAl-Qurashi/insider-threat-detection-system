# Scenario 1 — Simulation Commands (PLACEHOLDER)

**STATUS: not yet filled in.**

Scenario 1's six-stage narrative (off-hours access, access outside job
role, mass file copy, steganography, USB, log tampering) was built and
verified in an earlier working session not fully available here.

Whoever has it — please add the actual commands/actions used for each
of the six stages on Employee-Insider, matching the format of
[`scenario2-commands.md`](scenario2-commands.md): the exact command run,
and which rule ID + evidence it's expected to produce on the dashboard.

Reference: the engineering notebook, Sections 15.2 and 15.5, has the
verified rule IDs and debugging history for each stage — this file
should document the *commands*, which the notebook doesn't fully spell out.
